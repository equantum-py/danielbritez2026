# Documentos

Agregar el CV definitivo como:

```text
public/documents/cv-daniel-britez.pdf
```

Después de subir el archivo, cambiar `cvAvailable` a `true` en `data/portfolio.ts` para activar el botón de descarga.
